{
  "name": "webhook-relay",
  "version": "1.0.0",
  "description": "Lightweight HTTP webhook relay with retry, filtering, and fan-out support",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "node --watch src/index.ts",
    "test": "node --experimental-test-runner test/**/*.test.ts",
    "lint": "eslint src/"
  },
  "keywords": ["webhook", "relay", "proxy", "http", "retry"],
  "author": "vein-systems",
  "license": "MIT",
  "dependencies": {
    "fastify": "^4.0.0",
    "pino": "^9.0.0",
    "undici": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
